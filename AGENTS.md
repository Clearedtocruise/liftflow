# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

## Stable baseline (STABLE_BASELINE tag)

- Branch `stable-baseline` restores pre–Build 67 production code plus font and EAS provisioning hotfixes.
- No new features until STABLE_BASELINE is deployed and verified on TestFlight.
- **One feature per build.** No build may contain more than one major feature.
- Every feature must survive one full TestFlight cycle before the next feature is started.
